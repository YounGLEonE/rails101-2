<% if flash.any? %>
  <% user_facing_flashes.each do |key, value| %>
    <div class="alert alert-dimissable alert-<%= flash_class(key) %>">
      <button class="close" date-dismiss="alert">×</button>
      <%= value %>
    </div>
  <% end %>
<% end %>    
